<?php
function isPrime($n) {
    if ($n<=0){
        echo "FALSE";
        return false;
    }
    for ($i=2; $i<=$n/2; $i++) {
        if ( $n%$i == 0 ){
            echo "FALSE";
            return false;
        }
    }
    echo "TRUE";
    return true;
}

echo isPrime(0);
